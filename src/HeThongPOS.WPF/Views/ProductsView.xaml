<Page x:Class="HeThongPOS.WPF.Views.ProductsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Quản Lý Sản Phẩm" Background="#f3f4f6">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="200" Margin="0,0,10,0" Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" />
            <ComboBox Width="150" Margin="0,0,10,0" ItemsSource="{Binding Categories}" DisplayMemberPath="TenDanhMuc" SelectedItem="{Binding SelectedCategory}" />
            <Button Content="Tìm kiếm" Command="{Binding SearchCommand}" Width="80" Margin="0,0,10,0"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã Vạch" Binding="{Binding MaVach}" Width="150"/>
                <DataGridTextColumn Header="Tên Sản Phẩm" Binding="{Binding TenSanPham}" Width="*"/>
                <DataGridTextColumn Header="Danh Mục" Binding="{Binding DanhMuc.TenDanhMuc}" Width="150"/>
                <DataGridTextColumn Header="Giá Bán" Binding="{Binding GiaBan, StringFormat={}{0:N0}}" Width="120"/>
                <DataGridTextColumn Header="Tồn Kho" Binding="{Binding TonKho}" Width="80"/>
                <DataGridTextColumn Header="ĐVT" Binding="{Binding DonViTinh}" Width="80"/>
                <DataGridCheckBoxColumn Header="Trạng Thái" Binding="{Binding TrangThai}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Thêm mới" Command="{Binding AddProductCommand}" Width="100" Margin="0,0,10,0" Style="{StaticResource PrimaryButton}"/>
            <Button Content="Chỉnh sửa" Command="{Binding EditProductCommand}" Width="100" Margin="0,0,10,0"/>
            <Button Content="Xóa" Command="{Binding DeleteProductCommand}" Width="100" Background="#FF4444" Foreground="White"/>
        </StackPanel>
    </Grid>
</Page>
