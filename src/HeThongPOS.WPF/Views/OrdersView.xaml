<Page x:Class="HeThongPOS.WPF.Views.OrdersView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Lịch Sử Đơn Hàng" Background="White">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Thanh tìm kiếm -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <TextBox Grid.Column="0" Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                     Padding="12" FontSize="14" BorderBrush="#d1d5db" BorderThickness="1"
                     Tag="Tìm mã đơn hoặc tên khách..."/>
        </Grid>

        <!-- Danh sách đơn hàng -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding OrdersList}" SelectedItem="{Binding SelectedOrder}"
                  AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#e5e7eb"
                  Background="White" RowHeight="45" FontSize="14" BorderThickness="1" BorderBrush="#e5e7eb">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã Đơn" Binding="{Binding MaDonHang}" Width="150" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="Thời Gian" Binding="{Binding NgayTao, StringFormat={}{0:dd/MM/yyyy HH:mm}}" Width="160"/>
                <DataGridTextColumn Header="Khách Hàng" Binding="{Binding KhachHang.HoTen, TargetNullValue='Khách lẻ'}" Width="*"/>
                <DataGridTextColumn Header="Thu Ngân" Binding="{Binding NhanVien.HoTen}" Width="*"/>
                <DataGridTextColumn Header="Tổng Tiền" Binding="{Binding TongThanhToan, StringFormat={}{0:N0} đ}" Width="150">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="Foreground" Value="#059669"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Trạng Thái" Binding="{Binding TrangThai}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
