<UserControl x:Class="HeThongPOS.WPF.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header and Filters -->
        <TextBlock Text="Danh sách Đơn hàng" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryColor}" Margin="0,0,0,15"/>
        
        <Border Grid.Row="1" Style="{StaticResource CardBorder}">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Filter Bar -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBlock Text="Từ ngày:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextSecondaryColor}"/>
                    <DatePicker SelectedDate="{Binding FromDate, UpdateSourceTrigger=PropertyChanged}" Width="140" Margin="0,0,20,0"/>

                    <TextBlock Text="Đến ngày:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextSecondaryColor}"/>
                    <DatePicker SelectedDate="{Binding ToDate, UpdateSourceTrigger=PropertyChanged}" Width="140" Margin="0,0,20,0"/>

                    <TextBlock Text="Trạng thái:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextSecondaryColor}"/>
                    <ComboBox ItemsSource="{Binding OrderStatuses}" SelectedItem="{Binding SelectedStatus}" Width="140" Margin="0,0,20,0"/>

                    <Button Content="Tìm kiếm" Command="{Binding SearchCommand}" Width="100" Margin="0,0,10,0" Style="{StaticResource PrimaryButton}"/>
                    <Button Content="Xóa bộ lọc" Command="{Binding ClearFilterCommand}" Width="100" Style="{StaticResource OutlinedButton}"/>
                </StackPanel>

                <!-- Orders DataGrid -->
                <DataGrid Grid.Row="1" ItemsSource="{Binding Orders}" SelectedItem="{Binding SelectedOrder}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã Đơn" Binding="{Binding MaDonHang}" Width="120"/>
                        <DataGridTextColumn Header="Khách Hàng" Binding="{Binding KhachHang.HoTen}" Width="180"/>
                        <DataGridTextColumn Header="Ngày Tạo" Binding="{Binding NgayTao, StringFormat={}{0:dd/MM/yyyy HH:mm}}" Width="160"/>
                        <DataGridTextColumn Header="Tổng Tiền" Binding="{Binding TongThanhToan, StringFormat={}{0:N0} ₫}" Width="140">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="Margin" Value="0,0,20,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Trạng Thái" Binding="{Binding TrangThai}" Width="120"/>
                        <DataGridTextColumn Header="Ghi Chú" Binding="{Binding GhiChu}" Width="*"/>
                    </DataGrid.Columns>
                    <DataGrid.InputBindings>
                        <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding ViewDetailCommand}"/>
                    </DataGrid.InputBindings>
                </DataGrid>

                <!-- Bottom Action Bar -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button Content="Xem chi tiết" Command="{Binding ViewDetailCommand}" Width="140" Style="{StaticResource PrimaryButton}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
