<UserControl x:Class="HeThongPOS.WPF.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Danh sách Khách hàng" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryColor}" Margin="0,0,0,15"/>

        <Border Grid.Row="1" Style="{StaticResource CardBorder}">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBox Width="300" Margin="0,0,15,0" Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" />
                    <Button Content="Tìm kiếm" Command="{Binding SearchCommand}" Width="100" Style="{StaticResource PrimaryButton}"/>
                </StackPanel>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                        <DataGridTextColumn Header="Họ Tên" Binding="{Binding HoTen}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Số Điện Thoại" Binding="{Binding SoDienThoai}" Width="150"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                        <DataGridTextColumn Header="Điểm Tích Lũy" Binding="{Binding DiemTichLuy}" Width="120"/>
                        <DataGridTextColumn Header="Ngày Tạo" Binding="{Binding NgayTao, StringFormat={}{0:dd/MM/yyyy}}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button Content="Thêm mới" Command="{Binding AddCustomerCommand}" Width="120" Margin="0,0,15,0" Style="{StaticResource PrimaryButton}"/>
                    <Button Content="Chỉnh sửa" Command="{Binding EditCustomerCommand}" Width="120" Margin="0,0,15,0" Style="{StaticResource OutlinedButton}"/>
                    <Button Content="Xóa" Command="{Binding DeleteCustomerCommand}" Width="120" Style="{StaticResource OutlinedButton}" Foreground="{StaticResource DangerColor}" BorderBrush="{StaticResource DangerColor}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
