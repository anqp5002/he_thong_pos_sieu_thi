<UserControl x:Class="HeThongPOS.WPF.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="250" Margin="0,0,10,0" Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" Padding="5" />
            <Button Content="Tìm kiếm" Command="{Binding SearchCommand}" Width="80" Margin="0,0,10,0"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Họ Tên" Binding="{Binding HoTen}" Width="*"/>
                <DataGridTextColumn Header="Số Điện Thoại" Binding="{Binding SoDienThoai}" Width="150"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                <DataGridTextColumn Header="Điểm Tích Lũy" Binding="{Binding DiemTichLuy}" Width="120"/>
                <DataGridTextColumn Header="Ngày Tạo" Binding="{Binding NgayTao, StringFormat={}{0:dd/MM/yyyy}}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Thêm mới" Command="{Binding AddCustomerCommand}" Width="100" Margin="0,0,10,0" />
            <Button Content="Chỉnh sửa" Command="{Binding EditCustomerCommand}" Width="100" Margin="0,0,10,0"/>
            <Button Content="Xóa" Command="{Binding DeleteCustomerCommand}" Width="100" Background="#FF4444" Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>
