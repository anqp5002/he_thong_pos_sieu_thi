<Page x:Class="HeThongPOS.WPF.Views.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="768" d:DesignWidth="1024"
      Title="LoginView"
      Background="#f0f2f5">

    <Grid>
        <Border Background="White" Width="400" Height="450" 
                CornerRadius="8" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect Color="#d1d5db" BlurRadius="10" ShadowDepth="2" Opacity="0.5"/>
            </Border.Effect>
            
            <StackPanel Margin="30">
                <TextBlock Text="Đăng Nhập" FontSize="28" FontWeight="Bold" Foreground="#1a73e8" 
                           HorizontalAlignment="Center" Margin="0,10,0,30"/>
                
                <TextBlock Text="Tên đăng nhập:" Margin="0,0,0,5" Foreground="#374151" FontWeight="SemiBold"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                         Height="40" Padding="10,0" Margin="0,0,0,15"
                         VerticalContentAlignment="Center" FontSize="14"
                         BorderBrush="#d1d5db" BorderThickness="1"/>
                
                <TextBlock Text="Mật khẩu:" Margin="0,0,0,5" Foreground="#374151" FontWeight="SemiBold"/>
                <PasswordBox x:Name="PasswordBox"
                             Height="40" Padding="10,0" Margin="0,0,0,20"
                             VerticalContentAlignment="Center" FontSize="14"
                             BorderBrush="#d1d5db" BorderThickness="1"
                             PasswordChanged="PasswordBox_PasswordChanged"/>
                
                <!-- Error Message -->
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" 
                           TextWrapping="Wrap" Margin="0,0,0,15"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}, FallbackValue=Collapsed}"/>
                
                <Button Content="{Binding IsBusy, Converter={StaticResource BooleanToLoginTextConverter}}" 
                        Command="{Binding LoginCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Height="45" FontSize="16"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
