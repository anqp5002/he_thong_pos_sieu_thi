<UserControl x:Class="HeThongPOS.WPF.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.expression/blend/2008" 
             xmlns:controls="clr-namespace:HeThongPOS.WPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Quản Lý Nhân Viên" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryColor}"/>
                <TextBlock Text="Xem danh sách tài khoản và kích hoạt/khóa tài khoản thu ngân" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"/>
            </StackPanel>
        </Grid>

        <!-- Top Tool Bar: Search & Action Buttons -->
        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBox Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}" 
                             Width="300" Height="36" Margin="0,0,10,0"
                             Tag="Nhập tên hoặc tên đăng nhập..."/>
                    <Button Command="{Binding LoadDataCommand}" Content="🔍 Tìm kiếm" Style="{StaticResource OutlinedButton}" Height="36" Padding="15,0"/>
                </StackPanel>

                <!-- Actions -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <!-- Toggle Status Button -->
                    <Button Command="{Binding ToggleUserStatusCommand}" 
                            Style="{StaticResource PrimaryButton}" 
                            Background="#D97706"
                            Height="36" Padding="20,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔑 " FontSize="12" VerticalAlignment="Center"/>
                            <TextBlock Text="Khóa / Mở khóa" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Table Grid -->
        <Border Grid.Row="2" Style="{StaticResource CardBorder}">
            <Grid>
                <DataGrid ItemsSource="{Binding Users}" 
                          SelectedItem="{Binding SelectedUser}"
                          AutoGenerateColumns="False" 
                          IsReadOnly="True" 
                          SelectionMode="Single"
                          BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã số" Binding="{Binding Id}" Width="80"/>
                        <DataGridTextColumn Header="Tên đăng nhập" Binding="{Binding Username}" Width="150"/>
                        <DataGridTextColumn Header="Họ và tên" Binding="{Binding HoTen}" Width="*"/>
                        <DataGridTextColumn Header="Chức vụ" Binding="{Binding VaiTro.TenVaiTro}" Width="150"/>
                        
                        <!-- Status Badge Column -->
                        <DataGridTemplateColumn Header="Trạng thái" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border x:Name="StatusBadge" CornerRadius="12" Padding="10,4" HorizontalAlignment="Left" Background="#E5F9F6">
                                        <TextBlock x:Name="StatusText" Text="Đang hoạt động" Foreground="#10B981" FontSize="11.5" FontWeight="SemiBold"/>
                                    </Border>
                                    <DataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding TrangThai}" Value="False">
                                            <Setter TargetName="StatusBadge" Property="Background" Value="#FEE2E2"/>
                                            <Setter TargetName="StatusText" Property="Text" Value="Đã khóa"/>
                                            <Setter TargetName="StatusText" Property="Foreground" Value="#EF4444"/>
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Ngày tạo" Binding="{Binding NgayTao, StringFormat={}{0:dd/MM/yyyy HH:mm}}" Width="180"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Loading Spinner Overlay -->
                <Border Background="#80FFFFFF" Visibility="Collapsed" CornerRadius="8">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding LoadDataCommand.IsRunning}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <controls:LoadingSpinner HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
