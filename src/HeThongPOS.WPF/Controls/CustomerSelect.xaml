<UserControl x:Class="HeThongPOS.WPF.Controls.CustomerSelect"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignWidth="350">
    
    <Border Background="White" CornerRadius="6" Padding="15" BorderBrush="#e5e7eb" BorderThickness="1">
        <StackPanel>
            <TextBlock Text="Khách hàng (Tùy chọn)" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,10" Foreground="#374151"/>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox Grid.Column="0" Text="{Binding SearchCustomerKeyword, UpdateSourceTrigger=PropertyChanged}" 
                         Height="36" Padding="10,0" FontSize="14" VerticalContentAlignment="Center"
                         BorderBrush="#d1d5db" BorderThickness="1" Foreground="#111827"
                         IsEnabled="{Binding SelectedCustomer, Converter={StaticResource InverseBooleanConverter}}">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding SearchCustomerCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>

                <Button Grid.Column="1" Content="Tìm" Command="{Binding SearchCustomerCommand}"
                        Width="60" Height="36" Margin="5,0,0,0" Background="#f3f4f6" BorderBrush="#d1d5db"
                        Visibility="{Binding SelectedCustomer, Converter={StaticResource InverseBooleanConverter}}"/>

                <Button Grid.Column="1" Content="Xóa" Command="{Binding ClearCustomerCommand}"
                        Width="60" Height="36" Margin="5,0,0,0" Background="#fee2e2" Foreground="Red" BorderThickness="0"
                        Visibility="{Binding SelectedCustomer, Converter={StaticResource StringToVisibilityConverter}, FallbackValue=Collapsed}"/>
            </Grid>

            <!-- List gợi ý KH -->
            <ListBox ItemsSource="{Binding SuggestedCustomers}" 
                     Margin="0,5,0,0" BorderThickness="1" BorderBrush="#e5e7eb"
                     MaxHeight="150"
                     Visibility="{Binding SuggestedCustomers.Count, Converter={StaticResource StringToVisibilityConverter}}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding HoTen}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding SoDienThoai}" Foreground="#6b7280" FontSize="12"/>
                            </StackPanel>
                            <Button Grid.Column="1" Content="Chọn" Padding="10,5"
                                    Command="{Binding DataContext.SelectCustomerCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Khách hàng đã chọn -->
            <StackPanel Margin="0,10,0,0" Visibility="{Binding SelectedCustomer, Converter={StaticResource StringToVisibilityConverter}}">
                <TextBlock Text="Đã chọn:" FontSize="12" Foreground="#6b7280"/>
                <TextBlock Text="{Binding SelectedCustomer.HoTen}" FontWeight="Bold" FontSize="14" Foreground="#1a73e8"/>
                <TextBlock Text="{Binding SelectedCustomer.SoDienThoai}" FontSize="13" Foreground="#374151"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
